# 🔍 PROMETHEUS DEBUG QUERIES

## 1️⃣ ПРОВЕРЬ КАКИЕ МЕТРИКИ ВООБЩЕ ЕСТЬ:

### Базовый поиск:
```promql
# Все метрики начинающиеся с http
{__name__=~"http.*"}

# Все доступные метрики (осторожно, может быть много!)
{__name__=~".*"}

# Node.js процесс метрики
{__name__=~"process.*"}

# NestJS метрики (если есть)
{__name__=~"nest.*"}
```

## 2️⃣ ЕСЛИ МЕТРИК НЕТ - ПРОВЕРЬ TARGETS:

1. **Status** → **Targets**
2. Найди `api-gateway` target
3. Статус должен быть **UP**

## 3️⃣ ВОЗМОЖНЫЕ ПРИЧИНЫ:

### ❌ Метрики не настроены в API Gateway
### ❌ Prometheus не может достучаться до API Gateway  
### ❌ Endpoint /metrics недоступен
### ❌ Неправильный порт в prometheus.yml

## 4️⃣ АЛЬТЕРНАТИВНЫЕ НАЗВАНИЯ МЕТРИК:

Может быть метрики называются по-другому:
```promql
# Если используется другая библиотека
{__name__=~".*request.*"}
{__name__=~".*http.*"}
{__name__=~".*duration.*"}
```
