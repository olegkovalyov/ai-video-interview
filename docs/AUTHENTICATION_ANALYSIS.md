# 📊 Анализ текущей системы аутентификации

## 🔧 Бэкенд (API Gateway) - OAuth2 + JWT

### Роуты аутентификации:

#### `GET /auth/login` 
- **Назначение:** Инициирует OAuth2 flow с Authentik
- **Параметры:** `?redirect_uri=<callback_url>`
- **Возвращает:**
```typescript
{
  success: boolean;
  authUrl: string;        // URL для редиректа в Authentik
  state: string;          // OAuth2 state параметр
  redirectUri: string;    // Callback URL
}
```

#### `GET /auth/callback`
- **Назначение:** Обрабатывает OAuth2 callback от Authentik
- **Параметры:** `?code=<auth_code>&state=<state>&redirect_uri=<callback>`
- **Действия:** 
  - Обменивает код на токены
  - Устанавливает httpOnly cookies (access_token, refresh_token, id_token)
  - Публикует Kafka событие `user.authenticated`
- **Возвращает:**
```typescript
{
  success: boolean;
  expiresIn: number;     // Время жизни access_token в секундах
  tokenType: string;     // "Bearer"
}
```

#### `POST /auth/refresh`
- **Назначение:** Обновляет токены используя refresh_token
- **Источники refresh_token:** body JSON или cookies
- **Действия:**
  - Валидирует refresh_token в Authentik
  - Получает новые токены
  - Обновляет cookies
  - Публикует Kafka событие
- **Возвращает:**
```typescript
{
  success: boolean;
  expiresIn: number;
}
```

#### `POST /auth/logout` ✅ ОБНОВЛЕН
- **Назначение:** Выполняет полный logout с автоматическим редиректом
- **Действия:**
  - Валидирует access_token для получения userInfo
  - Отзывает токены в Authentik (access + refresh)
  - Очищает все auth cookies
  - Публикует Kafka событие `user.logged_out` с реальным userId
  - Строит End Session URL с id_token_hint и post_logout_redirect_uri
- **Возвращает:**
```typescript
{
  success: boolean;
  endSessionEndpoint?: string;   // Полный OIDC End Session URL
  requiresRedirect?: boolean;    // NEW: флаг необходимости редиректа
  idToken?: string;             // Для совместимости (deprecated)
}
```

#### `GET /protected` 
- **Назначение:** Тестовый защищенный роут
- **Защита:** `@UseGuards(JwtAuthGuard)`
- **Возвращает:** Информацию о пользователе из JWT

---

## 🎨 Фронтенд (Next.js) - Cookie-based

### Страницы аутентификации:

#### `/login` 
- Кнопка "Continue with Authentik"
- Вызывает `GET /auth/login` → редирект на Authentik

#### `/auth/callback`
- Обрабатывает возврат из Authentik
- Вызывает `GET /auth/callback` с query параметрами
- При успехе → редирект на `/dashboard`

#### `/register`
- Простая страница с редиректом на Authentik registration

### Защищенные страницы:
- `/dashboard/*` - все страницы дашборда
- **⚠️ НЕТ автоматической защиты через middleware!**

### Механизм проверки аутентификации:
```typescript
// В каждом защищенном компоненте:
useEffect(() => {
  const loadUserData = async () => {
    try {
      const res = await apiGet("/protected");  // Проверка сессии
      setUser(res.user);
    } catch (e) {
      if (e.message?.includes('401')) {
        router.replace("/login");  // Редирект при неавторизованности
      }
    }
  };
  loadUserData();
}, []);

// В dashboard компонентах:
const onLogout = async () => {
  if (isLoggingOut) return; // Предотвращение двойных кликов
  
  setIsLoggingOut(true);
  try {
    const response = await apiPost("/auth/logout");
    
    // Автоматический redirect в Authentik для полного logout
    if (response.requiresRedirect && response.endSessionEndpoint) {
      window.location.href = response.endSessionEndpoint;
      return; // Authentik сам перенаправит обратно через invalidation flow
    }
    
    router.replace("/"); // Fallback
  } catch (error) {
    router.replace("/"); // Надежный logout даже при ошибках
  } finally {
    setIsLoggingOut(false);
  }
};
```

---

## 🔄 Refresh Token механизм

### Бэкенд:
✅ **Реализован полностью**
- Endpoint: `POST /auth/refresh`
- Источники токена: body или httpOnly cookies
- Автоматическое обновление cookies
- Kafka событие при обновлении

### Фронтенд:
❌ **НЕ реализован**
- Нет автоматического обновления токенов
- Нет перехватчика HTTP запросов
- При истечении access_token → редирект на login

---

## 🍪 Управление токенами

### Хранение:
- `access_token` - httpOnly cookie
- `refresh_token` - httpOnly cookie  
- `id_token` - httpOnly cookie

### API запросы:
```typescript
// Все запросы включают cookies
fetch(url, {
  credentials: 'include'  // Автоматически отправляет cookies
});
```

---

## ⚡ Kafka интеграция ✅ ОБНОВЛЕНА

При аутентификации публикуются события:
```typescript
// При OAuth login/callback
{
  eventType: 'user.authenticated',
  payload: {
    userId: string,
    email: string,
    sessionId: string,
    authMethod: 'oauth2'
  }
}

// При refresh token
{
  eventType: 'user.authenticated', 
  payload: {
    userId: 'unknown',  // ⚠️ Все еще нет пользователя в refresh flow
    email: 'unknown',
    sessionId: string,
    authMethod: 'jwt_refresh'
  }
}

// ✅ НОВОЕ: При logout
{
  eventType: 'user.logged_out',
  payload: {
    userId: string,        // ✅ Реальный userId получен из access_token
    sessionId: string,
    logoutReason: 'user_action' | 'token_expired' | 'admin_action'
  }
}
```

---

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### ✅ ИСПРАВЛЕНО: Logout механизм 
- ✅ Полный OIDC logout с автоматическим редиректом
- ✅ Отзыв токенов в Authentik
- ✅ Публикация Kafka событий с реальным userId
- ✅ Настроен кастомный Invalidation Flow в Authentik
- ✅ Нет промежуточных страниц - прямой редирект на главную

### 1. Отсутствует middleware.ts
- ❌ Нет автоматической защиты роутов
- ❌ Каждый компонент проверяет аутентификацию вручную
- ❌ Дублирование кода проверки во всех dashboard компонентах

### 2. Нет автоматического refresh на фронте
- ❌ При истечении токена → logout вместо обновления
- ❌ Пользователь теряет сессию без необходимости
- ❌ Нет перехватчика запросов для автообновления
- ❌ Плохой UX - неожиданные разлогины

### 3. Неполная информация в refresh events
- ❌ `userId: 'unknown'` в Kafka событиях при refresh
- ❌ Сервисы не знают кто обновил токен
- ❌ Невозможно отследить активность пользователя при refresh

### 4. Нет централизованного auth state
- ❌ Каждый компонент управляет состоянием отдельно
- ❌ Дублирование логики проверки
- ❌ Нет единого источника истины о статусе авторизации
- ❌ Нет глобальных auth событий

### 5. Нет обработки сетевых ошибок
- ❌ При недоступности API Gateway → непредсказуемое поведение
- ❌ Нет retry механизма для критических запросов
- ❌ Нет fallback стратегий

### 6. Отсутствует проверка ролей/прав
- ❌ Нет role-based access control (RBAC)
- ❌ Все авторизованные пользователи имеют одинаковые права
- ❌ Нет защиты на уровне компонентов/роутов по ролям

### 7. Неполная интеграция с TypeScript
- ❌ Нет типов для пользователя и ролей
- ❌ Слабая типизация auth состояния
- ❌ Отсутствуют интерфейсы для auth API

### 8. Отсутствует loading states
- ❌ Нет индикаторов загрузки при проверке авторизации
- ❌ Мерцание при начальной загрузке защищенных страниц
- ❌ Плохой UX при медленных соединениях

---

## 🎯 ПЛАН РЕФАКТОРИНГА

### ✅ ЗАВЕРШЕНО:
- ✅ **Полный logout механизм** с OIDC End Session
- ✅ **Автоматический редирект** без промежуточных страниц
- ✅ **Kafka события logout** с реальным userId
- ✅ **Кастомный Invalidation Flow** в Authentik
- ✅ **Отзыв токенов** при logout

### Приоритет 1 (КРИТИЧНО)
1. **Создать `middleware.ts`** - автоматическая защита dashboard роутов
2. **Реализовать auto-refresh токенов** с перехватчиком запросов
3. **Добавить auth context/provider** для централизованного состояния
4. **Исправить refresh token events** - передавать реальный userId

### Приоритет 2 (ВАЖНО)
5. **Добавить типы TypeScript** для auth системы
6. **Создать auth hooks** (`useAuth`, `useUser`, `useProtected`)
7. **Добавить loading/error states** для auth компонентов
8. **Реализовать retry логику** для сетевых ошибок

### Приоритет 3 (ЖЕЛАТЕЛЬНО)
9. **Добавить RBAC систему** с ролями и правами
10. **Создать auth утилиты** для проверки прав
11. **Добавить auth debugging** и логирование
12. **Создать auth тесты** (unit + integration)

---

## 📁 ФАЙЛЫ ДЛЯ СОЗДАНИЯ/ИЗМЕНЕНИЯ

### Frontend (Next.js):
```
apps/web/
├── middleware.ts                     # NEW - защита роутов
├── app/lib/
│   ├── auth.ts                      # NEW - auth утилиты  
│   ├── auth-context.tsx             # NEW - auth provider
│   └── hooks/
│       ├── useAuth.ts               # NEW - основной auth hook
│       ├── useUser.ts               # NEW - пользователь hook  
│       └── useProtected.ts          # NEW - защищенные страницы
├── app/components/
│   ├── AuthProvider.tsx             # NEW - обертка приложения
│   ├── ProtectedRoute.tsx           # NEW - защищенный роут
│   └── auth/
│       ├── LoginForm.tsx            # REFACTOR
│       ├── LoadingSpinner.tsx       # NEW
│       └── AuthError.tsx            # NEW
└── types/
    └── auth.ts                      # NEW - типы auth
```

### Backend (API Gateway):
```
apps/api-gateway/src/auth/
├── auth.service.ts                  # REFACTOR - улучшить refresh
├── jwt-auth.guard.ts               # REFACTOR - добавить роли
├── roles.decorator.ts              # NEW - роли декоратор
├── roles.guard.ts                  # NEW - проверка ролей
└── dto/
    ├── auth-user.dto.ts            # NEW
    └── refresh-token.dto.ts        # NEW
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Сценарии для тестирования:
1. **Login flow** - OAuth2 + callback + cookies
2. **Auto-refresh** - истечение токена + автообновление
3. **Logout flow** - очистка токенов + редирект
4. **Protected routes** - доступ только для авторизованных
5. **Network errors** - retry + fallback поведение
6. **Concurrent requests** - множественные запросы с refresh
7. **Role-based access** - разные права для разных ролей

### Метрики для мониторинга:
- Успешность автообновления токенов
- Время отклика auth проверок
- Количество разлогинов по истечению
- Ошибки auth API
- Kafka события аутентификации

---

## 📝 ЗАМЕТКИ

### Текущее состояние:
- ✅ Базовая OAuth2 аутентификация работает
- ✅ JWT токены корректно проверяются
- ✅ Kafka интеграция функционирует
- ✅ Cookie-based session management
- ✅ **НОВОЕ:** Полный OIDC logout с автоматическим редиректом
- ✅ **НОВОЕ:** Кастомный Authentik Invalidation Flow
- ✅ **НОВОЕ:** Kafka события logout с реальным userId
- ✅ **НОВОЕ:** Отзыв токенов при logout
- ❌ Отсутствует фронтенд auto-refresh
- ❌ Нет централизованного auth state
- ❌ Слабая защита роутов

### Технический долг:
- Дублирование auth логики в компонентах
- Отсутствие TypeScript типов
- Неполная обработка ошибок
- Нет auth тестов
- ~~Отсутствие документации по auth flow~~ ✅ **ИСПРАВЛЕНО:** Создан полный гайд по Authentik

### Authentik настройки:
- ✅ **OAuth2 Provider** с правильными Redirect URIs
- ✅ **Application** связанное с Provider
- ✅ **Redirect Stage** для автоматического редиректа на frontend
- ✅ **Кастомный Invalidation Flow** с User Logout + Redirect Stages
- ✅ **Provider Invalidation Flow** настроен на кастомный flow
- ✅ **Тестовый пользователь** для аутентификации
- ✅ **Документация** по полной настройке Authentik

Этот анализ будет использоваться как руководство для пошагового рефакторинга системы аутентификации.
